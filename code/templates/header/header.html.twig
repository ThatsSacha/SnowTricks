{% block header %}

    <header>
        <a class="logo" href="{{ path('index') }}">
            SnowTricks
        </a>
        <ul>
            <li>
                <a href="{{ path('index') }}">Accueil</a>
            </li>
            <li>
                <a href="{{ path('index') }}">Tricks</a>
            </li>
            {% if app.user %}
                <li>
                    <a href="{{ path('app_login') }}">Compte <small>({{ app.user.pseudo }})</small></a>
                </li>
                <li>
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li>
                    <a href="{{ path('user_new') }}">Inscription</a>
                </li>
            {% endif %}
        </ul>
    </header>

{% endblock %}
