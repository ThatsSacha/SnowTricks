{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">
        <i class="bi bi-box-arrow-down"></i>
        {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
