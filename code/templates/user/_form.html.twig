{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">
        <i class="bi bi-person-plus"></i>
        {{ button_label|default('S\'inscrire') }}
    </button>
{{ form_end(form) }}
